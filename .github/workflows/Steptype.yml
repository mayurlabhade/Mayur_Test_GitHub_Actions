name: testing step types
on:
  workflow_dispatch:
jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo "Hello from Linux"
      - uses: actions/setup-python@v4
        with:
          python-version: '3.13'
      - name: step3
        run : print ('Hello from python')
        shell: python
