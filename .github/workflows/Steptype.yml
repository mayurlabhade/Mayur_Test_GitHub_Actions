name: Dependacy Check Between Job
on:
  workflow_dispatch:
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo "Hello from Linux, This is Build Job"
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: step2
        run: echo "This Job is Test Job"
